#include <stdio.h>
#include <conio.h> //_getch()
#include <Windows.h> // _kbhit(), GetAsy¾îÂ¼±¸ÀúÂ¼±¸

int playerX = 0;
int playerY = 0;

int main()
{
	while (1)
	{
		if (_kbhit)
		{
			if (GetAsyncKeyState(VK_LEFT));
		}
	}
}
